<?php
require_once "includes/securesession.inc.php";
require_once "includes/databasis.inc.php";

$result = $sqliconn->query("SELECT * FROM products WHERE category = 'shirt'");

// Close the database connection
$sqliconn->close();
?>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="css_files/main.css">
    <link rel="stylesheet" href="css_files/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        /* Add your styles for card layout here */
        .card {
            border: 1px solid #ccc;
            padding: 10px;
            margin: 10px;
            width: 200px; /* Adjust the card width as needed */
            display: inline-block;
            vertical-align: top;
        }

        .card img {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>

<body>
    <!-- NAVIGATION BAR -->
    <?php include "header.php"; ?>

    <!-- BANNER SECTION -->
    <section id="banner-box">
        <div class="banner-text">
            <h1>Catalog of Sweaters</h1>
        </div>
    </section>

    <!-- BODY SECTION -->
    <section id="catalog-container">
    <?php
    // Check if there are products in the result
    if ($result && $result->num_rows > 0) {
        // Loop through each row in the result set
        while ($row = $result->fetch_assoc()) {
            // Display each product as a card
            echo '<div class="card">';
            echo '<h2>' . $row['product_name'] . '</h2>';
            echo '<p>' . $row['product_info'] . '</p>';
            echo '<p>Price: $' . $row['price'] . '</p>';
            echo '<p>Stock: ' . $row['stock'] . '</p>';
            echo '<img src="' . $row['img'] . '" alt="' . $row['product_name'] . '">';
            echo '<a href="productshowcase.php?product_id=' . $row['product_id'] . '">View Details</a>';
            echo '</div>';
        }
    }
    ?>
    </section>

    <!-- FOOTER -->
    <?php include "footer.php"; ?>

</body>

</html>
